# ============================================
# 城市空气质量(AQI)实时监测与预测分析系统
# Python 依赖包清单
# ============================================

# ----- GUI 框架 -----
customtkinter>=5.2.0

# ----- 数据可视化 -----
matplotlib>=3.7.0
seaborn>=0.13.0

# ----- 数据处理 -----
pandas>=2.1.0
numpy>=1.24.0

# ----- 数据库（Python 内置，无需安装） -----
# sqlite3 —— Python 标准库自带

# ----- 工具类 -----
Pillow>=10.0.0          # 图像处理，CustomTkinter 图标支持
requests>=2.31.0        # HTTP 请求，用于获取在线 AQI 数据（预留）
PyYAML>=6.0.0           # YAML 配置文件解析
openpyxl>=3.1.0         # 读取 Excel 文件所需依赖
tkcalendar>=1.6.1       # 日历选择器控件
openai>=1.0.0           # DeepSeek API 客户端（OpenAI兼容）
